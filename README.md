Voting Dapp is a full-stack voting application built to make creating and running polls simple and transparent. The project is split into two parts:

client — the front-end interface where users create polls and cast votes.
vote_app — the backend service handling poll data, vote submission, and results.

Whether you're running a quick community poll or a more structured election, Voken aims to provide a lightweight, easy-to-deploy voting solution.

A couple of things worth flagging:

The repo currently has no description, README, or topics set, so I inferred the purpose mainly from the name "Voken" and the client/vote_app folder structure — you may want to confirm this matches your actual feature set.
If Voken involves blockchain/token-based voting (given the name and that some of your other repos are dApps), let me know and I'll rewrite the description to highlight that angle instead.